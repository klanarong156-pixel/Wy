# ผลตรวจหน้าแดชบอร์ดจากเบราว์เซอร์ (Historical)

> เอกสารนี้เป็นบันทึกการตรวจวันที่ 2026-08-14 และ **ไม่ใช่ source of truth ของระบบปัจจุบัน** ข้อความเรื่องโหมด, pin map, access.js หรือปัญหา asset ให้ตรวจเทียบกับ `README.txt`, `MQTT_CONTRACT_V6.md`, `BUILD_STATUS.txt` และ source ที่ใช้งานจริงก่อนนำไปปฏิบัติ

วันที่ตรวจ: 2026-08-14

หน้า `index.html` โหลด HTML และสคริปต์ได้บางส่วน แต่ภาพโลโก้ `logo.png` แสดงขนาดใหญ่มากจนกินพื้นที่ viewport แรก ทำให้เนื้อหาหลักและการควบคุมถูกเลื่อนลงไปมาก ควรปรับ hero/brand image ให้เป็นภาพประกอบขนาดควบคุมได้และให้ผู้ใช้เห็นสถานะระบบตั้งแต่แรกเข้า

เมื่อโหลดหน้าแบบ `file://` พบว่า MQTT เชื่อมต่อและได้รับข้อมูลจริงจาก ESP8266 ในเซสชันเบราว์เซอร์: อุณหภูมิ 28.2–28.3 °C, ความชื้นประมาณ 74–80%, สถานะ ESP8266 ออนไลน์, โหมด MANUAL และข้อมูลพยากรณ์ Open-Meteo แสดงผลได้ แสดงว่าการไหลของข้อมูลหลักทำงานในสภาพแวดล้อมนี้

พบข้อผิดพลาดเชิงข้อมูลในข้อความหน้าเว็บ: ส่วน Hardware ระบุ DHT11 = D2 และ DS3231 = D3/D4 แต่เอกสารและ `config.js` ระบุ DHT11 = D0, DS3231 SDA = D2, SCL = D1 จึงต้องแก้ให้ตรงกันทั้งหน้าเว็บและเอกสาร

พบความเสี่ยงเพิ่มเติมจากการตรวจไฟล์: หลายหน้ามีการอ้างอิง `access.js` แต่ไฟล์ถูกลบจาก HEAD ปัจจุบัน ทำให้ระบบบังคับยืนยันตัวตนและการตรวจ role ไม่ทำงานตามที่ออกแบบไว้

## ผลตรวจหน้าตั้งเวลา

หน้า `schedule.html` แสดงโครงสร้างตาราง 4 ช่วงเวลาและปุ่มบันทึก/ลบ แต่การจัดวางและขนาดตัวอักษรผิดเพี้ยนอย่างรุนแรงในเบราว์เซอร์ จึงจำเป็นต้องสร้างระบบสไตล์ใหม่ทั้งหน้า ไม่ใช่เพียงปรับเล็กน้อย

จากการเทียบ source พบปุ่มในหน้าเรียก `switchSchedTab()`, `saveSchedule()` และ `deleteSchedule()` แต่ไม่มีนิยามฟังก์ชันเหล่านี้ในโค้ดปัจจุบัน ทำให้ฟังก์ชันหลักของหน้าใช้งานไม่ได้ ส่วน `schedule-fix.js` มีเฉพาะ `removeScheduleSlot()`

คอนโซลหน้า schedule ไม่แสดงข้อความเพิ่มเติมระหว่างการตรวจ แต่สคริปต์ที่หาย (`access.js`) และฟังก์ชัน inline ที่ไม่มีการนิยามเป็นข้อบกพร่องเชิงโครงสร้างที่ตรวจยืนยันจาก source แล้ว

## สรุปจุดบกพร่องที่ยืนยันแล้ว

| ระดับ | จุดบกพร่อง | ผลกระทบ |
| --- | --- | --- |
| สูง | `access.js` ถูกลบ แต่หน้าที่ต้องล็อกอินและ service worker ยังอ้างถึง | การบังคับยืนยันตัวตน บทบาท และ logout ใช้งานไม่ได้ |
| สูง | หน้าตั้งเวลาไม่มีตรรกะสลับแท็บ/บันทึก/ลบ | ตั้งตารางอัตโนมัติไม่ได้ |
| สูง | หน้า settings/admin เรียก `mqttHandler.showSetup()` และ `clearCredentials()` แต่โมดูลไม่มีเมธอด | จัดการข้อมูลเชื่อมต่อ MQTT ไม่ได้ |
| สูง | MQTT username/password ถูกเก็บใน `config.js` ฝั่งผู้ใช้ | ความลับถูกเผยแพร่แก่ผู้เปิดหน้าเว็บทุกคน |
| กลาง | `sw.js` และ `manifest.json` อ้าง `access.js` และ `icon-512.png` ที่ไม่มีอยู่ | การติดตั้งหรือแคช PWA อาจล้มเหลว/คงไฟล์ผิดไว้ |
| กลาง | เอกสาร `HARDWARE_V6.md`/`README.txt`/`config.js` ขัดกับ firmware และหน้า index เรื่อง pin | ผู้ใช้เดินสายหรือแก้ปัญหาฮาร์ดแวร์จากข้อมูลที่คลาดเคลื่อนได้ |
| กลาง | การตีความเวลา Open-Meteo ไม่มี timezone offset ชัดเจน | การตัดสิน Rain Protection ในช่วง 6 ชั่วโมงอาจคลาดเคลื่อน |
| กลาง | หน้า finance โหลด `fonts/noto-thai.js` แต่ไฟล์จริงอยู่ root | การทำ PDF ภาษาไทยไม่ครบถ้วน |
| ต่ำ | มีโมดูล legacy (`stock.js`) พึ่ง global Firebase SDK ที่ไม่ได้โหลด | โค้ดตายและเสี่ยงต่อความสับสนในการดูแล |

## ผลตรวจหน้าการเงิน

หน้า `finance.html` แสดงผลโครงสร้างการเงินได้ แต่คอนโซลยืนยันว่าโหลดข้อมูลล้มเหลวด้วยข้อความ `กรุณาเข้าสู่ระบบ Firebase` ขณะที่หน้าต่างไม่ได้เปลี่ยนเส้นทางไปหน้า login อย่างชัดเจน ทำให้ผู้ใช้เห็นยอด `0` โดยไม่มีคำอธิบายสาเหตุที่เพียงพอ

หน้าใช้ Tailwind CDN ซึ่งคอนโซลเตือนว่าไม่เหมาะกับ production และยังอ้าง `fonts/noto-thai.js` ที่ไม่มีอยู่จริง จึงต้องย้ายสไตล์ที่จำเป็นมาเป็นไฟล์ภายในและแก้พาธของฟอนต์ก่อนส่งออก PDF

## ผลตรวจหน้าเข้าสู่ระบบ

หน้า `auth.html` แสดงผลโดยไม่มีสไตล์สำคัญ ทำให้รูปแบบสี การเว้นระยะ และการเข้าถึงอินพุตไม่อยู่ในมาตรฐานเดียวกับหน้าอื่น ต้องย้ายหน้าเข้าสู่ระบบเข้าสู่ระบบดีไซน์เดียวกันและเพิ่มข้อความแสดงสถานะในระหว่างส่งคำขอ/ผิดพลาด อย่างไรก็ตามคอนโซลไม่แสดงข้อผิดพลาดเมื่อเพียงโหลดหน้า

## ผลทดสอบหลังปรับปรุง: แดชบอร์ด V7

หน้า `index.html` เวอร์ชันใหม่แสดงผลได้ถูกต้องทั้ง desktop layout และข้อมูลอากาศจริงจาก Open-Meteo โดยไม่พบโลโก้ขนาดใหญ่บดบังเนื้อหาเหมือนเวอร์ชันเดิม ส่วน Rain Protection แสดงสถานะ `AUTO พร้อมใช้งาน` พร้อมค่าที่คำนวณได้จากหน้าต่าง 6 ชั่วโมง และปุ่ม AUTO ถูกเปิดใช้งานเมื่อตรงตามเงื่อนไข

ในรอบตรวจนี้ MQTT และ ESP8266 ไม่ได้เชื่อมต่อ เนื่องจาก credentials ถูกนำออกจาก source ตามมาตรการความปลอดภัยใหม่ จึงไม่สามารถยืนยันการควบคุมรีเลย์กับอุปกรณ์จริงได้โดยไม่ให้ผู้ปฏิบัติงานกรอกบัญชี MQTT ในหน้าเว็บ

## ผลทดสอบหลังปรับปรุง: หน้าตั้งเวลา V7

หน้า `schedule.html` ใหม่แสดงผลได้ดีและมีองค์ประกอบตั้งเวลา 4 ช่วงต่ออุปกรณ์ครบถ้วน อย่างไรก็ตาม ในการเปิดผ่าน `file://` หน้ายังไม่เปลี่ยนเส้นทางไป `auth.html` แม้ก่อนหน้านี้ตรวจพบการไม่มี session Firebase จึงต้องตรวจสอบ lifecycle ของ `access.js` ต่อด้วยคอนโซลและสถานะ local storage ก่อนสรุปการทดสอบ access control

การตรวจจาก console ในหน้า `schedule.html` ยืนยันว่า `smartfarm.firebase.user`, token และ refresh token ไม่มีค่า และ `SMARTFARM_ACCESS` ยังเป็น null แม้ `data-auth-required` เป็น `true` จึงไม่ใช่กรณี session คงค้าง แต่เป็นการที่ตัวเริ่มต้นใน `access.js` ไม่ถูกเรียกหลัง DOM พร้อมใช้งาน

การตรวจซ้ำพบว่าเบราว์เซอร์โหลด access logic เวอร์ชันใหม่แล้ว แต่ `SMARTFARM_ACCESS` ยังคง null สาเหตุเชิงโครงสร้างคือ `firebase.js` ประกาศ `FirebaseAuth` และ `FirebaseDB` แบบ top-level const แต่ไม่ได้เผยแพร่เป็น `window.FirebaseAuth` และ `window.FirebaseDB` ขณะที่ `access.js` ตรวจ property เหล่านี้ก่อนเริ่มงาน จึงออกจาก `init()` ทันที

## ผลทดสอบหลังแก้ access control

หลังเผยแพร่ `FirebaseAuth` และ `FirebaseDB` บน `window` และบังคับโหลดไฟล์เวอร์ชันใหม่ การเปิด `schedule.html` โดยไม่มี session เปลี่ยนเส้นทางไป `auth.html?next=schedule.html…` ได้ถูกต้อง หน้า login ใหม่แสดงผลสมบูรณ์และคอนโซลไม่พบ runtime error ในรอบทดสอบนี้

## ผลทดสอบหน้าต่าง MQTT

บนแดชบอร์ด V7 ปุ่ม “ตั้งค่าบัญชี” เปิดหน้าต่างกรอก MQTT username/password ได้ถูกต้อง พร้อมตัวเลือกจดจำบนอุปกรณ์นี้และข้อความแจ้งว่าไม่บันทึกข้อมูลลงซอร์สโค้ด การตรวจครั้งนี้หยุดก่อนกรอกหรือบันทึก credentials จึงไม่มีการเชื่อมต่อหรือส่งคำสั่งไปยังอุปกรณ์จริง

## ผลทดสอบ PWA ผ่าน HTTP

แดชบอร์ด V7 โหลดผ่าน HTTP ได้พร้อมข้อมูล Open-Meteo และ `navigator.serviceWorker.ready` ยืนยันว่า service worker มีสถานะ active ภายใต้ scope `http://127.0.0.1:8080/` จึงยืนยันได้ว่า manifest และ cache namespace ใหม่สามารถลงทะเบียนได้ในบริบท PWA
