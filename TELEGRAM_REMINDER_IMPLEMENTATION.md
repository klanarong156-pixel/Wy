# Telegram Crop Reminder — วิธีใช้งานและขอบเขต

## ภาพรวม

ระบบนี้ใช้วิธีให้ ESP8266 จัดตารางและส่ง Telegram เอง จึงยังส่งเตือนได้เมื่อปิด Dashboard ตราบใดที่อุปกรณ์มี Wi‑Fi, เวลา RTC/NTP ถูกต้อง และตั้งค่า Bot Token กับ Chat ID ไว้แล้ว

ระบบไม่เปลี่ยน MQTT broker, WebSocket URL, username/password flow, Firebase Authentication, relay topics, timer topics หรือ schedule topics เดิม เพิ่มเพียง topic สำหรับข้อมูล reminder และไฟล์ LittleFS แยกจาก credential เดิม

## ขั้นตอนตั้งค่า

1. เปิดหน้า **ตั้งค่า** แล้วกรอก Telegram Bot Token และ Chat ID จากนั้นกด **บันทึกลง ESP8266**
2. กด **ส่งข้อความทดสอบ** เพื่อตรวจว่าอุปกรณ์ส่งข้อความไป Telegram ได้
3. เปิดหน้า **ตั้งเวลา** แล้วเลื่อนมาที่ **แจ้งเตือนงานรอบปลูก**
4. เปิดใช้งานการแจ้งเตือน เลือกจำนวนวันล่วงหน้า กำหนดเวลาส่ง และตั้ง **ช่วงเงียบ** เช่น เตือน `1 วัน` เวลา `18:00` และไม่ส่งระหว่าง `22:00–07:00`
5. หากใช้หลายแปลง ให้เพิ่มชื่อแปลงในส่วน **จัดการหลายแปลง** ก่อน แล้วเลือกแปลงตอนสร้างงาน
6. กรอกชื่องาน วันที่ต้องทำ รายละเอียด และเลือก **ทำซ้ำทุก** หากเป็นงานประจำ เช่น ใส่ปุ๋ยทุก 7 วัน
7. กด **บันทึกงาน** ระบบจะบันทึกไว้ใน browser/Firebase และส่งสำเนา active jobs ไปเก็บใน ESP8266

## ตัวอย่างผลลัพธ์

ถ้างานครบกำหนดวันที่ 8 สิงหาคม และตั้งเตือนล่วงหน้า 1 วัน เวลา 18:00 ESP8266 จะส่งข้อความในวันที่ 7 สิงหาคม เวลา 18:00 ว่า:

```text
[SmartFarm SmartFarm-ESP8266]
แจ้งเตือนงานรอบปลูกล่วงหน้า

กำหนดพรุ่งนี้: ใส่ปุ๋ยครั้งที่ 2
ครบกำหนด: 2026-08-08
รายละเอียด: ปุ๋ยละลายช้า 1 ช้อนโต๊ะต่อต้น
```

## การจัดการงาน

ผู้ใช้สามารถทำเครื่องหมายงานว่าเสร็จแล้ว แก้ไขงาน ปิดเฉพาะการเตือน เปิดกลับ ลบงาน และเลื่อนงานที่เลยกำหนดไปอีก 1 วันได้ งานที่เลยกำหนดจะเตือนซ้ำทุกวันก็ต่อเมื่อเปิดตัวเลือก **เตือนซ้ำทุกวันเมื่องานเลยกำหนด**

เมื่อกำหนด **ทำซ้ำทุก N วัน** และ ESP8266 ส่ง Telegram สำเร็จ ระบบจะเลื่อนวันครบกำหนดของงานนั้นไปอีก N วันโดยอัตโนมัติ งานจึงเป็นรอบถัดไปต่อเนื่องโดยไม่ต้องกดสร้างใหม่ ส่วนการกด **ทำเสร็จแล้ว** จะปิดงานรอบปัจจุบันตามปกติ การส่งสำเร็จของงานซ้ำจะส่งสถานะ `sent_recurring` กลับมาเพื่อให้ Dashboard แสดงวันรอบถัดไป

ESP8266 รองรับรายการ reminder สูงสุด 8 งานในรุ่นนี้ แต่ละงานมีรหัส ชื่อ วันที่ครบกำหนด จำนวนวันล่วงหน้า หมายเหตุ แปลงที่เกี่ยวข้อง ความถี่ซ้ำ สถานะเปิด/ปิด สถานะเสร็จ และวันที่ส่งล่าสุดเพื่อป้องกันการส่งซ้ำในวันเดียวกัน ข้อมูลประวัติ telemetry และสถิติระยะยาวเก็บฝั่ง browser/Firebase ไม่เพิ่มภาระหน่วยความจำให้ ESP8266

ช่วงเงียบเป็นช่วงเวลาท้องถิ่นของ ESP8266 หากเวลาปัจจุบันอยู่ในช่วงนั้น ระบบจะพักการส่งและตรวจใหม่ในรอบถัดไป หลังพ้นช่วงเงียบ หากยังเป็นวันที่เข้าเงื่อนไขและยังไม่เคยส่ง ระบบจะพยายามส่งให้ภายในวันนั้น กรณีตั้งเวลาเริ่มต้นเท่ากับเวลาสิ้นสุด ให้ถือว่าไม่มีช่วงเงียบ

## การทำงานเมื่ออุปกรณ์ออฟไลน์

ถ้าอุปกรณ์ออฟไลน์ตรงเวลาที่ต้องเตือน ระบบจะไม่สามารถส่งข้อความในขณะนั้นได้ แต่หากกลับมาออนไลน์ภายในวันเดียวกันหลังเวลาที่ตั้งไว้ ระบบจะตรวจพบและพยายามส่งภายหลังในวันนั้น หากไม่มี Wi‑Fi หรือ Telegram credential ระบบจะเก็บงานไว้และแสดงสถานะ `send_failed` เมื่อ Dashboard เชื่อมต่อกลับมา สถานะ `sent` หรือ `sent_recurring` หมายถึง ESP8266 ได้รับคำตอบสำเร็จจาก Telegram แล้ว ไม่ได้ยืนยันว่าผู้รับอ่านข้อความแล้ว

## MQTT additions

| รายการ | Topic | ลักษณะ |
|---|---|---|
| ส่งคำสั่ง reminder | `smartfarm/reminder/set` | non-retained JSON command |
| รับสถานะ reminder | `smartfarm/reminder/status` | retained JSON status |

คำสั่งที่รองรับคือ `settings`, `upsert`, `done`, `snooze`, `delete`, `sync` และ `test` รายละเอียด payload อยู่ใน `MQTT_CONTRACT_V6.md` โดยค่าช่วงเงียบส่งเป็น `quietStartHour`, `quietStartMinute`, `quietEndHour` และ `quietEndMinute` เพื่อให้ตรงกับ firmware

## MQTT connection continuity

ทุกหน้าที่โหลด `mqtt-handler.js` จะพยายามใช้ `mqtt-shared-worker.js` เพื่อถือ WebSocket connection เดียวร่วมกันระหว่างหน้าเว็บ ทำให้การเปลี่ยนเมนูภายในเว็บไม่ต้องเริ่ม connection ใหม่ทุกหน้า ถ้า browser ไม่รองรับ SharedWorker ระบบจะ fallback ไปใช้ connection แบบเดิมโดยอัตโนมัติ

การคง connection นี้มีผลขณะยังมีหน้าเว็บของแอปเปิดอยู่เท่านั้น หากปิดทุกแท็บแล้วเปิดใหม่ ย่อมต้องสร้าง connection ใหม่หนึ่งครั้งตามปกติ แต่ไม่ต้องกรอก credential ใหม่ถ้าเลือกจดจำบัญชีไว้

## ไฟล์ที่เพิ่มหรือแก้

ส่วน UI อยู่ใน `index.html`, `schedule.html`, `settings.html` และ `app.css` ส่วน logic หน้าเว็บอยู่ใน `crop-reminders.js`, `crop-plots.js`, `farm-analytics.js` และ `farm-tools.js` ส่วน connection bridge อยู่ใน `mqtt-shared-worker.js` ส่วน scheduler และ Telegram delivery อยู่ใน `SmartFarm_V6_PRODUCTION1.ino` และ contract อยู่ใน `MQTT_CONTRACT_V6.md`

การสำรองข้อมูลจากหน้า **ตั้งค่า** จะเก็บข้อมูลรอบปลูก งานเตือน แปลง และ analytics ที่ไม่เป็นความลับลงไฟล์ JSON โดยจงใจไม่รวม MQTT password, Firebase token, Telegram Bot Token หรือ credential ใด ๆ การนำเข้าจะเขียนกลับ local storage และ Firebase เฉพาะเมื่อผู้ใช้ล็อกอินอยู่

> ก่อนใช้งานจริงต้อง compile และอัปโหลด firmware จาก `SmartFarm_V6_PRODUCTION1.ino` ไปยัง ESP8266 ก่อน ระบบหน้าเว็บอย่างเดียวจะยังไม่ทำให้ ESP8266 ส่ง reminder ได้ หลังอัปโหลดให้ทดสอบ Telegram, รีเลย์ และช่วงเงียบกับอุปกรณ์จริง เนื่องจาก static test ไม่สามารถยืนยันการทำงานของปั๊ม/รีเลย์หรือการส่งข้อความผ่านเครือข่ายจริงได้
