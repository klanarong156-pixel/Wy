Smart Farm V7.1 Field Stability build and validation status

The most recent historical CI evidence in this repository predates the V7.0 source changes. It confirmed that the previous ESP8266 source compiled successfully, but it is not evidence that the modified V7.0 firmware compiles or behaves correctly.

Current source changes requiring validation
- Firmware version string is now V7.1.0-FIELD-STABILITY with bounded recurrence, plot labels, quiet-hour scheduler settings and field diagnostics.
- Field-stability patch adds bounded Telegram event queue, additive heartbeat diagnostics and an OTA safe state that forces all relays OFF while firmware is being written; reminder/test messages retain their existing synchronous result behavior.
- Firmware ไม่มี active AUTO/MANUAL topic; ตารางทำงานใน ESP8266 เมื่อ clock ถูกต้อง และ relay/timer เป็นคำสั่งโดยตรงตาม topic เดิม
- Emergency Stop latch ใหม่ปิดรีเลย์ ยกเลิก timer และบล็อกคำสั่ง ON จนกว่าจะได้รับ `EMERGENCY_RESET`; คำสั่ง OFF เดิมยังคงรองรับ
- Dashboard MQTT credentials were removed from source and must now be supplied by the operator in the browser.
- Dashboard pin-map content was aligned to the current firmware: DHT11=D2, DS3231 SDA=D3, DS3231 SCL=D4, relays D5–D8.

Required validation before production use
1. Compile SmartFarm_V6_PRODUCTION1.ino for esp8266:esp8266:nodemcuv2 with the project libraries.
2. Flash a test NodeMCU and verify boot state: all relays are OFF.
3. Verify MQTT connect, retained relay/timer/schedule/emergency statuses, and DHT11 heartbeat.
4. Verify all four schedule slots for every relay when the local clock is valid, including an overnight slot.
5. Verify `EMERGENCY_STOP` turns all relays OFF, cancels timers, blocks ON commands, and `EMERGENCY_RESET` restores the explicitly requested schedule behavior.
6. Verify that the pump follows the saved schedule and explicit relay/timer commands without an unrequested 30-minute or MQTT-loss cutoff; verify `CANCEL`, a long finite timer up to 71,582 minutes, and `UNLIMITED` separately.
7. Verify OTA with a stable power supply and the configured OTA password. Confirm all relays go OFF before writing, a failed upload does not reboot, and a successful upload reboots only after the HTTP response completes.
8. Verify Emergency Stop from the Dashboard, then test a separately wired physical E-stop/contactor with the real pump load.

The current local compile profile for the emergency-latch/timer patch is 49% global RAM (39,820/80,192 bytes), 94% IRAM (62,099/65,536 bytes) and 52% flash (550,448/1,048,576 bytes). IRAM headroom remains limited; avoid adding large firmware arrays or history buffers. The RTC path validates every `rtc.now()` result and verifies the read-back value after `rtc.adjust()`. The HTTP OTA path starts Update with max sketch space, forces relays OFF during the update and responds to Private Network Access preflight. Wi-Fi recovery clears stale MQTT/TLS state on Wi-Fi loss; the SharedWorker owns exponential MQTT reconnect backoff. Telegram event notifications use a four-entry bounded queue with a 15-second send interval. No 30-minute pump ceiling or automatic MQTT-loss pump cutoff is implemented.


Latest V7 CI verification
- Commit: 7cedf18c88d74a5ae602c238b41a274a8f0d4c27.
- Workflow: Smart Farm quality, run 31807405341.
- Web syntax/assets job: SUCCESS.
- ESP8266 firmware build job: SUCCESS in approximately 1 minute 5 seconds.
- Artifact: SmartFarm-V7-ESP8266-BIN.
- This CI result verifies compilation and static checks. It does not replace physical relay, sensor, Wi-Fi, MQTT, Telegram, RTC or OTA validation.
- Telemetry history and relay statistics remain in browser/Firebase; the ESP8266 stores only the bounded active reminder list and quiet-hour settings.
- Previous RTC-validated local artifact: `SmartFarm_V7.1.0-RTC-VALIDATED.bin`, SHA-256 `95798d5ea62d1c67df907d0cc5a4f7e493ad3b9b181e15cbca812d7f11f5ee5b`.
- Previous OTA-hardened local artifact: `SmartFarm_V7.1.0-OTA-HARDENED.bin`, SHA-256 `b6c4674f20e51ca38f85452a0dc18ec4eb7178fe37105ab4fada56c8f5f77dac`.
- Latest connection-stable local artifact: `SmartFarm_V7.1.0-CONNECTION-STABLE.bin`, SHA-256 `ab4ab7232f511c56c0e0188afc1c40db69e7ad38889eec98310db223c98e3331`.
- Latest OTA reboot-fix local artifact: `SmartFarm_V7.1.0-OTA-REBOOT-FIX.bin`, SHA-256 `30ae95d0e744ae52f479f41177a6d650648a8622afce321a283ee7bd38e394f6`. It stops MQTT/TLS and Telegram side effects before streaming, verifies total upload bytes against written bytes, sends the HTTP response with Connection close, and delays reboot by 1500 ms after a successful finalize.
- Previous field-stability local artifact: `SmartFarm_V7.1.0-FIELD-STABILITY.bin`, 591,968 bytes, SHA-256 `92bf3db1efd3765c0193e76a2790ea72fcae4ee689fce85ca20bc3be16d32398`.
- Current local artifact: `SmartFarm_V7.1.0-FIELD-STABILITY-EMERGENCY-LATCH.bin`, 593,376 bytes, SHA-256 `8bb987e10fc147afc7b4b2e7e6148abbd6655161148cfea484e953b6a6c18e7c`.
- Current compile profile: 39,820/80,192 bytes global RAM (49%), 62,099/65,536 bytes IRAM (94%) and 550,448/1,048,576 bytes flash (52%).
- Local checks: JavaScript syntax, JSON validation, MQTT contract audit and dashboard smoke test 62/62 passed; Firebase Realtime Database emulator parsed the rules successfully.
